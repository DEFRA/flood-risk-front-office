<%= backto_start_link %>

<%= render 'shared/errors' %>

<%= form_for @enrollment, html: { novalidate: true } do |form| %>


  <%= puts enrollment_partial_location(@enrollment.state) %>

    <%= render enrollment_partial_location(@enrollment.state), form: form %>

    <%= form.submit "Continue", class:'button' %>

<% end %>
